# Copy to .env.local and fill in once a Supabase project exists.
# Not used yet — Phase 1's frontend still reads static content from
# src/content/*.ts. These are placeholders for when client wiring starts.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
